(function() {

    var errorView = {};
      
      errorView.initErrorPage = function(err) {
          $('.container').hide();
          $('.error-view').show();
          // empty error-message
      };
    }());